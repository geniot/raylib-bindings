# Raylib Bindings

This submodule contains the binding generator for raylib. It's really scuffed,
and won't work for any other libraries. It's also not very well documented, so
good luck soldier.

#### Soldier:
It's ok; I only have 70 compilation errors to fix manually when using it for version 6.0 (was 5.0). No plans to fix the generator, probably I'll write my own one day.
